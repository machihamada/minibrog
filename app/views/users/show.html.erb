 <div class="contents row">
  <p><%= @nickname %>さんの投稿一覧</p>
  <% @brogs.each do |brog| %>
    <%= render partial: "brogs/brog", locals: { brog: brog } %>
  <% end %>
  <%= paginate(@brogs) %>
</div>
